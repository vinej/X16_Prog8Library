# Third-party emulator + ROM (not ours to redistribute)
/emulator/

# Build output (generated PRGs, BANK.BIN images, listings, temporaries)
/build/

/prog8-sdk/

# user-supplied image inputs / converted outputs
*.jpg
*.jpeg
*.png
*.bmx
*.vice-mon-list

# SD card images: half a gigabyte each, and someone else's files
*.img
*.img.old

# the desktop's saved icon list -- one per machine, not shared
DESKTOP.CFG

__pycache__/
